package eu.codlab.lorcana.math

import kotlin.test.Test

class TestDeck {
    @Test
    fun testDeckCreation() {
        /*
        val deck = Deck(
            id = "test",
            name = "test",
            deckSize = 64,
            defaultHand = 7
        )

        val scenario = Scenario("test")
        deck.addScenario(scenario)
        scenario.add("test")
        var valid = deck.scenario(
            "test",
            amount = 20,
            min = 20,
            max = 19
        )

        assertEquals(44, deck.others.amount)
        assertEquals(0, deck.others.min)
        assertEquals(-13, deck.others.max)

        assertFalse(valid, "the value is expected to be false")

        valid = deck.update(
            "test",
            amount = 8,
            min = 4,
            max = 8
        )

        assertTrue(valid)
        assertEquals(56, deck.others.amount)
        assertEquals(0, deck.others.min)
        assertEquals(3, deck.others.max)

        println("now calculate the result ${deck.calculate()}")
        assertEquals(0.326, deck.calculate(), 0.01)

        println("now adding a new card...")
        deck.add("test2")
        valid = deck.update(
            "test2",
            amount = 4,
            min = 1,
            max = 4
        )

        assertEquals(52, deck.others.amount)
        assertEquals(0, deck.others.min)
        assertEquals(2, deck.others.max)

        assertTrue(valid, "the value is expected to be true")

        assertEquals(0.0652, deck.calculate(), 0.01)

        println("now calculate the result ${deck.calculate()}")
         */
    }
}
